#include <stdio.h>
#include "hash_pessoa.h"
#include <stdbool.h>


int main(int argc, char **argv) {
  void inserir (dicionario_t *d, char *chave, pessoa_t *valor);
    printf("insira seu usuario, *chave);
    scanf("%9s" *chave);

return 0;